# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local data — never commit real participant data
data/local/

# Offline map archives. Large (tens to hundreds of MB), specific to wherever a
# course is running, and downloaded separately. See docs/offline-maps.md.
data/basemap/*.pmtiles
*.db
*.sqlite
*.sqlite3

# Node / Expo (mobile app, added in a later stage)
node_modules/
.expo/
dist/
*.log

# Secrets
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.vscode/
.idea/
