# Everything the dashboard needs except the hexagon map.
#
# Use this when `pip install -r requirements.txt` fails on h3, which happens on
# platforms with no prebuilt wheel for it — Termux on Android is the case that
# comes up in practice.
#
# What you lose: the "Whole course" aggregate view, and with it the k-anonymity
# slider demonstration. Everything else — the live map, participant days and
# weeks, pattern of life, groups, corroboration, teardown — works normally, and
# the aggregate screen explains itself rather than failing blankly.
#
# What you keep: an install that takes seconds rather than compiling a C++
# build system on a tablet.

Flask==3.1.3          # the web server
gunicorn==23.0.0      # production web server, used when hosting
